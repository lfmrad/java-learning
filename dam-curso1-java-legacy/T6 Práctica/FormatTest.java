import java.text.NumberFormat;

public class FormatTest {
    public static void main(String[] args) {
        NumberFormat formatTest = new NumberFormat();

        // Eva T6 dice que es setFormat(); que no existe! es format();
        // formatTest.setMisHuevos();
        formatTest.format();
    }
}